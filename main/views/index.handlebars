<h2>Home</h2>
<div>
    {{#each categories}}
    {{#if this.cars}}
    <div>
        <h3>
            <a href="{{this.path}}" />{{this.name}}</a>
        </h3>
        {{#each this.cars}}
        <div>
            <a href="{{../this.path}}/car-{{this._id}}">
                {{this.name}}
            </a>
        </div>
        {{/each}}
    </div>
    {{/if}}
    {{/each}}
</div>